// 1. Hole dir alle wichtigen Elemente aus dem HTML
// Tipp: Nutze document.querySelectorAll('.maulwurf'), um alle Maulwürfe in einer Liste zu speichern.


// 2. Erstelle Variablen für den Spielzustand (Score, Zeit, ob das Spiel läuft)


// 3. Funktion: Ein zufälliges Loch auswählen
function zufaelligesLoch() {
    // Tipp: Erzeuge eine Zufallszahl zwischen 0 und der Anzahl der Löcher
}

// 4. Funktion: Einen Maulwurf auftauchen lassen
function zeigeMaulwurf() {
    // - Wähle ein zufälliges Loch
    // - Füge dem Maulwurf darin die Klasse 'hoch' hinzu
    // - Nutze setTimeout, um die Klasse nach z.B. 800ms wieder zu entfernen
}

// 5. Funktion: Das Spiel starten
function startSpiel() {
    // - Setze Score auf 0
    // - Starte einen Timer (setInterval), der zeigeMaulwurf() aufruft
    // - Starte einen Countdown für die Spielzeit
}

// 6. Klick-Event für die Maulwürfe
// Tipp: Nutze eine Schleife (forEach), um jedem Maulwurf einen Event-Listener zu geben.
// Wenn geklickt wird: Score erhöhen und Klasse 'hoch' sofort entfernen.
